<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary x:Class="iSalesDeskPlus.Styles.SmallDevice"
                    xmlns="http://xamarin.com/schemas/2014/forms"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d">

    <OnPlatform x:Key="PageSpacing"
                x:TypeArguments="x:Double"
                Android="8"
                iOS="8" />
    <OnPlatform x:Key="PagePadding"
                x:TypeArguments="Thickness"
                Android="10"
                iOS="10" />

    <Style x:Key="LoginButtonStyle" TargetType="Button">
        <Setter Property="HeightRequest" Value="30" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="CornerRadius" Value="15" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="BackgroundColor" Value="{StaticResource Blue}" />
    </Style>

    <Style x:Key="LoginEntryStyle" TargetType="Entry">
        <Setter Property="FontSize" Value="13" />
    </Style>

</ResourceDictionary>