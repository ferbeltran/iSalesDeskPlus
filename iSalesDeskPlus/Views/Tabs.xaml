<?xml version="1.0" encoding="utf-8" ?>
    <TabbedPage x:Class="iSalesDeskPlus.Views.Tabs"
                xmlns="http://xamarin.com/schemas/2014/forms"
                xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
                xmlns:views="clr-namespace:iSalesDeskPlus.Views"
                Title="iSalesDesk+"
                prism:ViewModelLocator.AutowireViewModel="True"
                SelectedTabColor="{StaticResource PrimaryColor}"
                UnselectedTabColor="{StaticResource DarkerLightAccentColor}">


        <NavigationPage BarBackgroundColor="Transparent"
                        BarTextColor="{StaticResource PrimaryColor}"
                        IconImageSource="warehouse.png">

            <x:Arguments>
                <views:Inventory />
            </x:Arguments>
        </NavigationPage>


        <NavigationPage Title="Settings"
                        BarBackgroundColor="Transparent"
                        BarTextColor="{StaticResource PrimaryColor}"
                        IconImageSource="cog.png">

            <x:Arguments>
                <views:Settings />
            </x:Arguments>
        </NavigationPage>


    </TabbedPage>